# Deployment & Execution Guide

## 1. Local Execution (Native)
Ensure you have a C++20 compatible compiler (e.g., GCC 10+ or Clang 10+) installed.
```bash
# Compile the project
g++ -std=c++20 src/main.cpp src/twosum.cpp -o twosum_app

# Run the executable
./twosum_app